# 示例：每日 02:15 全量备份 MySQL 至 COS（与 backend PutBackupObject 对象键一致：db_save/年-月/日.sql.gz）
# 将 VINO_REPO改为服务器上本仓库路径；确保已安装 coscli 且 .env 含 DB_PASSWORD、COS_SECRET_ID、COS_SECRET_KEY。
#
# 15 2 * * * TZ=Asia/Shanghai cd /home/ubuntu/Vino_test && /usr/bin/env bash scripts/db_backup_to_cos.sh >>/var/log/vino-db-backup.log 2>&1

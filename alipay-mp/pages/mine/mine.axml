<view class="page">
  <view class="profile-header" onTap="onProfileTap">
    <view class="avatar">
      <image a:if="{{avatarUrl}}" class="avatar-img" src="{{avatarUrl}}" mode="aspectFill" />
      <text a:else class="avatar-text">{{avatarInitial}}</text>
    </view>
    <view class="profile-info">
      <text class="profile-name">{{isLoggedIn ? (userInfo.nickname || userInfo.username || '用户') : '点击登录'}}</text>
      <text class="profile-desc">{{isLoggedIn ? (userInfo.email || '') : '登录享更多权益'}}</text>
    </view>
  </view>

  <!-- 头像/昵称更新区域 -->
  <view class="edit-profile-card" a:if="{{isLoggedIn}}">
    <view class="edit-row" onTap="updateAvatar">
      <text class="edit-label">更换头像</text>
      <text class="edit-btn-text">选择头像 ›</text>
    </view>
    <view class="edit-row">
      <text class="edit-label">修改昵称</text>
      <input class="edit-nickname-input" placeholder="点击修改" value="{{userInfo.nickname}}" onBlur="onUpdateNickname" />
    </view>
  </view>

  <view class="stats-row">
    <view class="stat-item" a:for="{{stats}}" a:key="label">
      <text class="stat-num">{{item.value}}</text>
      <text class="stat-label">{{item.label}}</text>
    </view>
  </view>

  <view class="menu-card">
    <view
      class="menu-item"
      a:for="{{menus}}"
      a:key="title"
      data-index="{{index}}"
      onTap="onMenuTap"
    >
      <text class="menu-emoji">{{item.emoji}}</text>
      <text class="menu-title">{{item.title}}</text>
      <text class="menu-arrow">›</text>
    </view>
  </view>

  <view class="logout-area" a:if="{{isLoggedIn}}">
    <button class="btn-logout" onTap="logout">退出登录</button>
  </view>

  <view class="tab-bar-placeholder"></view>
</view>

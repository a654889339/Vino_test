# common/alipay-mp/cos_base

本仓库 R-Melamine 工程**当前无**支付宝小程序目录；Vino 侧支付宝端请优先

`require('../../common/frontend/cos_base/paths.js')`（以 `Vino_test/alipay-mp/utils` 为基准）。

仅当子包/路径无法上溯到 `common/frontend/cos_base` 时，可在此或项目内 alipay 专用目录下增加薄 re-export。配置真源仍见 `../config/cos` 与各后端 `GET /api/media/*` 约定。

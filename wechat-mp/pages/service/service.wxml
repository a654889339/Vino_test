<view class="page">
  <view class="guide-section">
    <view class="guide-header">
      <text class="guide-title">服务指南</text>
    </view>
    <scroll-view scroll-x class="guide-scroll">
      <view class="guide-card" wx:for="{{deviceGuides}}" wx:key="id" bindtap="showGuide" data-device="{{item}}">
        <view class="guide-icon" style="background: {{item.iconUrl ? '#fff' : item.gradient}}">
          <image wx:if="{{item.iconUrl}}" src="{{item.iconUrl}}" class="guide-icon-img" mode="aspectFit" />
          <text wx:else class="guide-emoji">{{item.emoji}}</text>
          <text class="guide-badge" wx:if="{{item.badge}}">{{item.badge}}</text>
        </view>
        <text class="guide-name">{{item.name}}</text>
        <text class="guide-model">{{item.model}}</text>
      </view>
    </scroll-view>
  </view>

  <view class="tabs">
    <view
      class="tab-item {{activeTab === index ? 'active' : ''}}"
      wx:for="{{tabs}}"
      wx:key="key"
      data-index="{{index}}"
      bindtap="switchTab"
    >
      <text>{{item.name}}</text>
    </view>
  </view>

  <view class="loading-wrap" wx:if="{{loading}}">
    <text>加载中...</text>
  </view>
  <view class="service-grid" wx:else>
    <view class="grid-item" wx:for="{{services}}" wx:key="id" bindtap="goServiceDetail" data-id="{{item.id}}">
      <view class="grid-icon" style="background: {{item.bg}}">
        <text class="grid-emoji">{{item.emoji}}</text>
      </view>
      <text class="grid-name">{{item.title}}</text>
      <text class="grid-desc">{{item.desc}}</text>
      <text class="grid-price">¥{{item.price}}起</text>
    </view>
  </view>

  <view class="tab-bar-placeholder"></view>
</view>
